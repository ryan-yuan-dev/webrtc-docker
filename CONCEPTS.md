# WebRTC 概念

## 构建系统

## CIPD

**CIPD**（全称 **Chrome Infrastructure Package Deployment**）是 Google 开发的一套跨平台软件包管理工具。是 Google 专为超大型项目打造的、“以内容寻址”的二进制文件分发系统。

它是 Chromium 项目（包括 WebRTC、V8 等）底层架构的核心组件，主要负责以**只读**、**可追溯**且**高可用**的方式，分发大规模项目编译时所需的二进制依赖、工具链和 SDK。

以下是理解 CIPD 的几个核心维度：

1. 它解决了什么问题？
    在大型项目（如 WebRTC）中，代码依赖于特定版本的编译器（如 Clang）、构建工具（如 GN、Ninja）或 SDK（如 JDK、Android NDK）。
    * **传统方式：** 将这些几百 MB 甚至 GB 级的二进制文件直接放入 Git 仓库。这会导致 Git 仓库体积迅速膨胀，拉取速度极慢。
    * **CIPD 方式：** Git 仓库中只存放一个轻量级的文本描述文件（Ensure file），记录了所需包的名称和版本号（Instance ID）。CIPD 负责根据这些信息从 Google 的服务器下载对应的二进制包并解压到指定目录。
2. 核心概念
    * **Package（包）：** 一个层级化的名称，例如 `infra/3pp/tools/ninja/linux-amd64`。它通过路径明确了包的功能、平台和架构。
    * **Instance（实例）：** 包的具体版本。CIPD 不使用语义化版本（如 v1.2.3），而是使用文件的 **SHA256 哈希值**。这意味着版本是不可变的：一旦发布，内容就绝对不会改变。
    * **Tag / Ref（标签/引用）：** 为了方便人类阅读，可以给哈希值贴上标签，比如 `version:2.17.0` 或 `latest`。

3. 工作流程

    当你运行 `gclient sync` 拉取 WebRTC 源码时，后台实际上发生了以下过程：
    1. **解析：** 检查项目中的 `.ensure` 文件。
    2. **验证：** 检查本地磁盘上是否已经存在对应哈希值的包。
    3. **下载：** 如果没有，则从 CIPD 后端下载压缩包。
    4. **部署：** 将包解压到工作区（例如 `src/third_party/jdk`），并将其设为只读以防误操作。

4. 为什么你在 WebRTC 构建中看到了它？

    WebRTC 的构建系统非常精密，为了保证全球开发者在不同机器上编译出的结果完全一致（确定性构建），它必须强制统一所有工具。

    你之前遇到的报错，本质上就是 CIPD 在尝试将**“云端的工具箱”**搬到**“你本地的货架”**上时，因为网络连接断开或超时，导致搬运失败了。

5. 常用命令（了解即可）

    如果你需要手动调试，常用的命令包括：
    * `cipd auth-login`: 登录（通常拉取公共包不需要）。
    * `cipd instances [package_name]`: 查看某个包的所有可用版本。
    * `cipd ensure -ensure-file [file] -root [dir]`: 根据配置文件同步包。

## gclient

## gn
