# frame_instrumentation_data_unittest

## 概述

`FrameInstrumentationData` 的单元测试，验证数据对象的字段设置和便捷方法：`SetSequenceIndex` 边界条件、`SetStdDev` 范围校验、`SetLumaErrorThreshold` 和 `SetChromaErrorThreshold` 阈值限制、`SetSampleValues` 合法性、`holds_upper_bits` 和 `is_sync_only` 逻辑。
