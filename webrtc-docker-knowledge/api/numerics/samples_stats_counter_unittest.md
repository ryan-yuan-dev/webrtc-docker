# samples_stats_counter_unittest

## 概述

对 `SamplesStatsCounter` 功能的完整单元测试。测试覆盖了基本统计（min/max/sum/average）、方差和标准差、百分位数计算、大量样本的数值稳定性、合并计数器以及乘除运算符。

在 "学习扩展" 和 "设计模式" 方面的内容已在 `samples_stats_counter.md` 中覆盖。
